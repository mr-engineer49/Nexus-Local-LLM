[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexus-local-llm"
version = "2.0.0"
dependencies = [
    "PyQt6>=6.5.0",
    "psutil>=5.9.0",
    "requests>=2.31.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["nexus*"]
